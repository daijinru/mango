# Mango Docker Client

This cli only runs locally, plz install docker at first, and run as follows:

```bash
# windows
$ docker-cli <flag> <args>
```

## images

List images:
```bash
$ docker-cli images
```

